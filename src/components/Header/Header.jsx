import React from 'react'
import './Header.css'

const Header = ({headTitle}) => {
    return (
        <div className="nav-container">
          <h1>{headTitle}</h1>
            
        </div>
    )
}

export default Header
